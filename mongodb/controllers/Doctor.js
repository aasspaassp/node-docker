export class Doctor {
    constructor(doctorAddress, specialty, name, birthYear) {
        this.doctorAddress = doctorAddress
        this.specialty = specialty
        this.name = name
        this.birthYear = birthYear
    }
}